# Required in production. Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Make.com Custom Webhook URL for the lead-scoring scenario. Treat it like a password.
# Required in production (the app refuses to start without it); in local dev, leads are logged instead.
MAKE_WEBHOOK_URL=

# Public origin, used for canonical URLs, Open Graph tags and sitemap.xml. No trailing slash.
# Required in production: the app refuses to start with a localhost value.
SITE_URL=http://localhost:5000

# Required in production: the app refuses to start with the .example placeholder.
CONTACT_EMAIL=hello@jengo.example

# Reverse proxies in front of the app. Railway and Render both use 1. Set 0 if nothing proxies the app.
TRUSTED_PROXY_COUNT=1

# Rate-limit storage. The default memory:// is per worker; set a Redis URL (redis://...) to share limits.
RATELIMIT_STORAGE_URI=memory://

# Add includeSubDomains to HSTS. Only set to 1 once every subdomain serves HTTPS.
HSTS_INCLUDE_SUBDOMAINS=0

# Set to 1 locally for auto-reload and debug pages.
FLASK_DEBUG=0
